<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite">

    <FrameLayout
        android:id="@+id/contentDashboardFramelayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorWhite"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/contentDashboardBottomnavigationView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/colorPrimary"
        android:elevation="@dimen/_10sdp"
        android:foreground="?attr/selectableItemBackground"
        android:theme="@style/Widget.BottomNavigationView"
        app:elevation="@dimen/_10sdp"
        app:itemIconTint="@drawable/bottom_nav_icon_color_selector"
        app:itemTextAppearanceActive="@style/BottomNavigation.InActiveItemTextAppearance"
        app:itemTextAppearanceInactive="@style/BottomNavigation.ActiveItemTextAppearance"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_navigation_view_menu" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>